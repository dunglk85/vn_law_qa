[tool.ruff]
target-version = "py311"
line-length = 120
exclude = ["scripts", "law-crawler", "seed"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
